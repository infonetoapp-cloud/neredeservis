app_identifier(ENV["IOS_BUNDLE_ID"] || "com.neredeservis.app")
apple_id(ENV["APPLE_ID"] || "infonetoapp@gmail.com")

itc_team_id(ENV["ASC_TEAM_ID"]) if ENV["ASC_TEAM_ID"]
team_id(ENV["APPLE_TEAM_ID"]) if ENV["APPLE_TEAM_ID"]

package_name(ENV["PLAY_PACKAGE_NAME"] || "com.neredeservis.app")
json_key_file(ENV["SUPPLY_JSON_KEY"]) if ENV["SUPPLY_JSON_KEY"]

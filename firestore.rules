rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // STEP-061 baseline: deny-all until callable-only access rules are hardened.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
